# Dev compose reads this file. Copy to .env and edit if you need overrides.
# All values below already have defaults in docker/dev/docker-compose.yml,
# so this file is OPTIONAL — only fill in what you want to override.

POSTGRES_DB=aic
POSTGRES_USER=aic
POSTGRES_PASSWORD=aic
POSTGRES_PORT=5433

N8N_PORT=5678
